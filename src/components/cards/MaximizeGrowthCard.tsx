import { MaximizeGrowthPropsType } from "@/@types/@types";

const MaximizeGrowthCard: React.FC<MaximizeGrowthPropsType["cards"][0]> = ({icon,title,description}) => {
    return (
        <div>
            Enter
        </div>
    );
}

export default MaximizeGrowthCard;